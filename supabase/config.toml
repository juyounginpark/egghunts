project_id = "leblcdiqsyxqzwlsnkio"

[auth]
site_url = "https://juyounginpark.github.io/egghunts/"
additional_redirect_urls = ["https://juyounginpark.github.io/egghunts/"]
# The default mail provider sends a magic link. Enable the code template only
# after configuring SMTP; otherwise Supabase rejects the entire Auth update.

[functions.game]
# Auth is verified with Auth /user in the function (publishable keys are not JWTs).
verify_jwt = false
