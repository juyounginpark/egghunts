# 알콩 원정대 작업 규칙

- 구현 전 `alkong-expedition-game-spec.md`와 `docs/qa/implementation-plan.md`를 확인한다.
- 사용자 변경사항을 보존한다. 요구사항을 크게 변경하거나 기존 작업을 삭제해야 하면 보고한다.
- 코드 위치 탐색에는 Semble search를 사용하고 결과 파일로 직접 이동한다. 전체 리터럴 참조가 필요할 때만 rg를 사용한다.
- 게임 성장 수치는 `src/data.ts`, 스테이지·공격 수치는 `src/stage-data.ts`에 모은다. 플랫폼 API는 `src/platform.ts` 어댑터 밖에서 호출하지 않는다.
- 변경 후 `npm run qa:fast`와 관련 E2E를 실행한다. 테스트 삭제·비활성화·근거 없는 기준 완화로 실패를 숨기지 않는다.
- UI 변경 시 모바일 스크린샷을 생성하고 실제 이미지를 확인한다. QA 기준 이미지는 의도한 변경을 검토한 후에만 갱신한다.
- 기본 환경/알 20³, 펫 10³~100³와 정수 픽셀 확대, 탐험 최소 HUD를 보존한다. 100³는 AIvoxel 부분 모델 합성. 복셀마다 Mesh를 만들지 않는다.
- 성능 예산: 초기 전송 4 MiB, JS gzip 300 KiB, draw calls 250, triangles 500,000. 브라우저 측정 FPS를 실기기 FPS로 보고하지 않는다.
- `npm run verify` 후 프로덕션 프리뷰 E2E를 수행한다. 실제 토스 인증·안전영역·리더보드는 실기기 검증 필요로 구분한다.
- 자동 수정은 동일 문제당 최대 3회. 남은 원인과 가설을 기록한다.
- 최신 출시 카탈로그와 기존 저장 데이터의 차이는 `docs/qa/stage-roadmap.md`에서 확인한다. 수집한 펫을 삭제해 종수를 맞추지 않는다.
- 가상 광고를 실제 수익화로, 로컬 게스트 멀티플레이를 공개 서버/정식 토스 계정 인증으로 보고하지 않는다.
- 최신 HP 개정은 기획서 앞부분을 따른다: 일반 피격 알 유지, 필드 무회복, 1초 무적, HP0 자동 귀환/70% 원정 XP. 공격 버튼과 광고 부활을 다시 활성화하지 않는다.
- 기존 35알/100펫 ID와 저장을 보존한다. 선택 차원(stage 1~20)과 기존 카탈로그 region은 별도 개념이다.

## Semble Code Search

- 체력 UI는 플레이어 머리 위 바를 사용한다. 최대 체력에서는 숨기며 손상·회복 전환과 카메라 추적을 E2E로 확인한다. 상단 체력 패널을 복구하지 않는다.
`mcp__semble__search`에 프로젝트 경로와 구현 의도를 전달한다. 문서는 `content="docs"`, 설정은 `content="config"`를 사용한다. MCP가 없으면 `semble search` 또는 `uvx --from "semble[mcp]==0.6.0" semble search`를 사용한다.
