# 알콩 원정대 — Codex 구현용 게임 기획·기술 명세서

## 최신 멀티플레이·시크릿 추가 (2026-09-24)

- 로그인 복구: SMTP 연결 전에는 실제 발송되는 기본 이메일 링크로 로그인한다. localhost 주소를 GitHub Pages 게임 주소로 수정하고 URL의 인증 세션을 SDK로 처리한다. 코드 입력은 실제 코드 수신 시에만 사용하는 접힌 선택 영역으로 둔다. SMTP 발송 제한 자체를 해제한 것은 아니다.

- 이메일 코드 인증 후 ‘방 찾기’로 빈자리 있는 방을 많이 찬 순서로 배정한다. 방당 최대 5명, 방 코드 없음. Supabase 프로젝트는 `leblcdiqsyxqzwlsnkio`다.
- 방마다 서버 난수로 다른 알을 생성하고 같은 방의 인원이 알 상태와 보스를 공유한다. 길은 기존 하나의 연속 탐험로다. 서버가 이동·획득 준비·알 소유·성장·수익·판매·강화를 계산한다. 재화나 개인 저장을 클라이언트에서 업로드하지 않는다.
- RLS로 직접 테이블 접근을 차단하고, 서버의 revision 비교 트랜잭션으로 알 상태와 개인 자원을 함께 저장한다. 기존 기기 저장은 보존하며 자동 합산하지 않는다.
- 기지를 5개 개인 농장과 운동기구가 있는 마을로 넓힌다. 기지 속도는 모두 기본 1.6. 자신의 농장 자산과 트레일만 조작할 수 있다.
- 각 스테이지에 전용 알로 부화하는 거대 시크릿 드래곤 1종을 추가한다. 펫 ID 300~319, 전용 알 variant 5. 기존 알 35개 ID·펫 300개 ID와 보유 기록을 보존한다. Secret 생성 확률은 유지하며 해당 등급의 절반을 새 전용 알로 배정한다. 드래곤 모델은 50³, 월드 배율 6.5다.
- 서버 배포·이메일 발송 설정 절차와 아직 확인하지 않은 운영 항목은 `docs/supabase-setup.md`에 기록한다. 별도 요청 없는 자동 게임 검증은 실행하지 않는다.

## 최신 캐릭터 개정 (2026-09-24)

- 사망 연출 추가: 플레이어는 약 0.8초 동안 옆으로 쓰러진 뒤 사망 시점부터 5초 동안 월드에 그대로 보인다. 축소·소멸시키지 않는다. 그 후 기존 5초 부활/복귀 선택 창을 표시하며 무선택이면 자동 복귀한다. 연출 중 이동·즉시 복귀·광고 시작은 막고, 동작 줄이기에서는 즉시 누운 자세로 전환한다. 플레이어 모델 자체는 변경하지 않는다.

- 대상은 펫 300종(기존 100종 + 스테이지 200종), 단계별 보스 20종과 최종 보스이다. 사용자 확인에 따라 플레이어는 변경하지 않는다.
- 펫·보스는 모두 50×50×50 이내의 오리지널 복셀 모델을 사용한다. 이전 펫 10³~100³ 및 100³ 부분 합성 규칙보다 이 개정을 우선한다. 기존 ID·보유 수·등급·능력·성장·게임 내 배율은 보존한다.
- 제작 정면은 -Z, 대칭축은 X=24.5이며 대응 좌표는 x ↔ 49-x이다. 게임 로더에서 정면을 기존 +Z 이동 방향으로 회전한다. 환경·알·플레이어 좌표는 바꾸지 않는다.
- 개별 콘셉트·실루엣·성격·생태·색은 `docs/art/character-concepts.md`, 편집 원본과 생성기는 `scripts/rework_characters.py`에 둔다. UI 아이콘은 실제 교체 모델을 렌더링한다.
- 이번 요청에서 명시한 정면·측면·후면·45도·작은 썸네일·대칭 검수는 수행한다. 검수 갤러리는 `docs/art/character-gallery.html`이다. 전체 게임 자동 QA의 기본 비실행 규칙은 유지한다.

> 작업 방식 개정 (2026-09-23): 사용자 요청으로 자동 검증 단계를 제거한다. 아래 문서의 QA·E2E·스크린샷·성능·verify·검증용 빌드 지시는 수동 실행 참고자료이며, 사용자가 검증을 명시적으로 요청할 때만 실행한다. 기존 테스트와 명령은 유지한다.

## 최신 사용자 개정: 밤 폐쇄·간결한 탐험·환경 장애물 (2026-09-23)

### 추가 개정: 터치 이동·희귀 알 획득·보스 기상·맵 충돌 (2026-09-24)

- 도감의 이전 모델·기존 100종 선택 옵션을 제거한다. 도감 카테고리는 스테이지 1~20만 제공하며 이전 카테고리 선택값 0은 현재 스테이지로 돌린다. 기존 펫 소유·발견·보상 저장은 보존한다.

- 최종 표시 개정: 행동·운동·하단 메뉴·특성 버튼은 공통 아이콘 중심으로 표시하고 이름은 접근성 이름·도움말로 유지한다. 수치와 필요한 경고는 남긴다. 픽셀 필터를 끄며 기존 정수 확대 규칙보다 이 개정을 우선한다. 월드를 CSS 화면 크기로 직접 렌더링하고 안티앨리어싱을 켠다. 기본 DPR은 기기 DPR과 1.5 중 작은 값, 저화질은 1을 사용한다. canvas와 이미지의 강제 pixelated 확대를 해제하되 복셀 모델·배경 에셋은 유지한다.

- 최신 피드백: 속도 카드는 속도계 SVG와 숫자로 표시한다. 펫 능력·자동 수익·합계는 터치/자동/속도/별가루 아이콘과 수치로 표시하며 상세 설명과 출처 배수는 접어서 보존한다. 새 이모티콘은 사용하지 않는다.
- 낮 종료 10초 전 상단 시간 영역을 밤 시작 카운트다운으로 바꾸며 마지막 3·2·1은 큰 숫자·주황색 강조·짧은 확대 효과를 적용한다. 동작 줄이기에서는 확대 효과를 생략한다. 밤에는 기존 아침까지 남은 초를 표시한다.
- 보스 기본 이동 속도는 해당 스테이지 권장 속도와 동일한 1.5×2^(stage-1)로 계산한다. 알 회수 3배와 알 운반 감속은 유지한다. 고단계 속도 증가로 피격 시 맵 끝까지 날아가지 않도록 넉백은 최대 24로 제한한다. 잠든 idle 보스만 2초 기상하며, 이미 깨어 있는 보스는 알 재습득 시 즉시 추격한다. 기상 도중 재습득은 남은 대기 시간을 초기화하지 않는다. 실제 둥지 복귀 후 잠든 상태는 다시 깨울 수 있다.

- 최신 메인 HUD: 상단 72px 카드와 8px 여백 안에 캐릭터·실제 레벨·경험치 바, 낮/밤 시각, 별가루·44px 설정 버튼을 모은다. 좁은 화면에서는 시간·별가루의 보조 문구만 줄을 나누고 큰 재화는 축약 표시한다. 농장명 패널은 작은 가장자리 라벨로 바꾼다. 이동 속도는 왼쪽 작은 운동 아이콘 카드에 ‘이동 속도 / 운동으로 증가’로 표시한다. 소모형 에너지를 새로 추가하지 않는다. 기존 농장·부화실·펫·강화·상점과 특성 기능을 하단에 모으고 조작 버튼 위치는 유지한다. 크림색·올리브색·황금색, 2px 테두리·8px 모서리·같은 방향 그림자를 사용한다.
- 사운드 설정: 배경음악·효과음 공통 볼륨 0~100% 슬라이더와 독립 음소거를 둔다. 변경은 즉시 적용·저장하며 음소거해도 선택한 볼륨을 보존한다. 기존 저장의 sound 설정을 유지하고 볼륨 누락은 기존 음량 100%로 보완한다.

- 최신 UI 조정: 조이스틱·배트·운동·알 행동 버튼을 64×64와 모서리 8px로 통일한다. 조이스틱은 왼쪽, 배트·알 행동은 오른쪽 기본 위치에 둔다. 운동하기는 오른쪽 배트 스윙 버튼 바로 위에 8px 간격으로 오른쪽 끝선을 맞춘다. 운동 버튼이 나타나도 기존 조작 위치는 밀리지 않는다. 떠 있는 조이스틱도 같은 크기를 쓴다. 메뉴·대화상자는 역할별 공통 높이·글자·모서리·간격을 적용한다. 낮·밤을 제외한 이모티콘은 게임 이미지, SVG 선 아이콘 또는 짧은 한글로 대체한다.
- 알 꺼내기 준비 중 연타는 취소하지 않는다. 시작할 때 이전 이동 입력을 초기화하고, 근처 다른 알의 우선순위가 바뀌어도 선택한 알을 유지한다. 이동·피격·사거리 이탈·대상 소멸 시에는 취소한다. 네트워크 알의 접근 범위는 서버 범위와 맞춘다.
- 추격 중 거리 경고 문구를 없애고 가까워질수록 붉은 화면 가장자리와 카메라 흔들림을 강화한다. 기상 2초 문구는 유지한다. 동작 줄이기에서는 흔들림을 끄며 일시정지·모달·사망·추격 종료 시 흔들림을 해제한다.

- 이동 조이스틱과 운동하기 버튼은 기본 위치를 유지한다. 게임 화면의 빈 곳을 누르면 조이스틱만 터치 위치로 옮겨지고 손을 떼거나 터치가 취소되면 기본 위치로 돌아온다. 조이스틱의 기본 공간을 유지해 운동하기 버튼이 밀리지 않게 한다. 버튼·메뉴·모달 터치와 구분하고 키보드 이동을 유지한다. 행동 버튼은 크게, 가까운 알의 실제 아이콘과 짧은 행동 문구를 표시한다. 반복 설명·장식용 영문·작은 보조 문구를 줄이고 운동 버튼도 그림 중심의 정사각형으로 표시한다.
- 첫 알에 접근하면 들기 전에 알 그림·보스 그림과 ‘알을 들면 보스가 쫓아와요!’ 경고, ‘큰 알일수록 이동 속도가 느려져요’ 안내를 한 번 표시한다. 확인 기록을 저장하며 알을 자동으로 줍지는 않는다. 운반 튜토리얼 제목에도 크기별 감속을 안내한다. 사용자 정정에 따라 ‘부모’라는 표현은 쓰지 않는다.
- 보스가 알을 들고 복귀할 때는 플레이어 대신 알을 놓을 둥지를 바라본다. 빈손 복귀 시에는 실제 이동 방향을 바라보며, 방향 전환은 부드럽고 빠르게 적용한다.
- S/SS/SSS/Secret 알은 1.5/3/5/7초 동안 제자리에서 꺼내며 큰 행동 버튼의 게이지로 진행을 표시한다. 이동·피격·대상 변경·화면 전환·일시정지 시 취소한다. C/B/A는 기존 즉시 습득을 유지한다. 알 목록·소유 기록·등급 확률은 보존한다. 희귀 알 운반 성공은 등급 색과 큰 획득 문구로 강조한다.
- 알 습득 후 ‘보스가 잠에서 깼어요!’를 2초 표시한다. 보스는 자던 실제 좌표에서 자세·크기·시선을 부드럽게 바꾸며 일어난 뒤 추격한다. 화면에만 적용하던 수면 위치 오프셋을 실제 상태로 옮겨 순간이동을 제거하고 이전 저장도 변환한다. 기존 5초 숫자 카운트·경고음 규칙을 대체한다.
- 희귀 알·펫은 꽃잎, 기계 궤도, 물결·진주, 불꽃·혜성, 별자리 계열의 입자를 사용한다. 출처·외형·펫 종별 색과 위상을 달리하고 SS는 바닥 문양, SSS는 빛의 날개, Secret은 상단 왕관·별 고리를 추가한다. 공유 재질과 InstancedMesh를 쓰며 저화질은 입자 수를 줄이고 동작 줄이기는 입자를 정지한다.
- 맵의 몸체 높이에 걸치는 나무 줄기·바위·구조물·기지 시설에 충돌 범위를 둔다. 장식 배치 데이터를 렌더링·충돌에서 공유하고 플레이어 이동·넉백은 연속 충돌과 벽면 미끄러짐으로 처리한다. 바닥 장식·머리 위 장식·부유 효과는 통과 가능하며 문 개구부·트레드밀 진입 공간을 남긴다. 기존 저장이 오브젝트 안에 있으면 빠져나올 수 있게 처리한다. 자동 테스트·빌드·시각 검증은 실행하지 않는다.

- 보스 알 회수 속도: 떨어진 자기 알로 이동하고 알을 둥지로 운반하는 동안에는 해당 보스 일반 이동속도의 3배를 적용한다. 최종 보스도 동일하다. 추격·빈손 복귀·공격 수치와 스테이지 밖 반출 알 회수 제외는 유지한다.

- 최신 밤 시간 조정: 밤 지속 시간을 45초에서 15초로 줄인다. 전체 180초 주기는 유지해 낮 165초·밤 15초로 운영한다. 밤 귀환·입구 폐쇄·아침 재개 및 재접속 시 공통 시간 계산에도 같은 지속 시간을 사용한다. 아래의 과거 45초 규칙보다 우선한다.

### 추가 개정: 알 크기별 운반 감속·1단계 추격

- 알 크기가 커지는 C/B/A/S/SS/SSS/Secret 순서대로 기본 운반 감속을 10/20/30/40/50/60/70%로 적용한다. 가장 큰 알은 운반 전 속도의 30%로 이동한다. 같은 크기는 출처 지역에 상관없이 같은 감속을 적용하며 기존 운반 강화·운반 특성의 감속 완화는 유지한다. 70% 상한은 알 운반 자체의 감속 기준이다.
- 최종 사용자 수정값 1.5를 적용한다. 1단계 보스 속도는 0.75→1.5, 권장 스피드 시작값은 3.2→1.5로 변경하며 이후 권장 스피드는 기존 단계당 2배 규칙(1.5, 3, 6, 12…)을 따른다. 나머지 보스 속도와 5초 각성 대기는 유지한다.

### 추가 개정: 기지 운동 바로가기·착용 펫 자동 수익

- 최신 수익 조정: 10초 수익 = 등급 기본 수익 × 펫 출처 스테이지 번호(1~20배). 현재 플레이어 위치가 아닌 펫의 고정 stageId를 사용한다. 출처 스테이지가 없는 기존 100종은 5개 카탈로그 region에 대해 각각 1/5/9/13/17배(지역군의 첫 단계 기준)를 적용한다. 펫 화면에 출처·배수·최종 수익을 표시하고 기존 미지급 적립량은 보존한다.

- 기지에서 정사각형 ‘운동하기’ 버튼을 표시한다. 누르면 트레드밀 중앙으로 이동해 운동을 시작하며 버튼을 숨긴다. 운동 중 ‘운동 그만하기’·‘운동 내리기’ 버튼은 표시하지 않고 조이스틱 이동으로 운동을 종료한다. 종료 후 기지에서는 운동 버튼을 다시 표시한다. 알 운반·사망·모달 중에는 시작하지 않는다.
- 착용한 펫은 플레이 중 10초마다 기존 재화 별가루(코인)를 자동으로 번다. C/B/A/S/SS/SSS/Secret 등급·희귀도별 1/2/4/8/16/32/64개이며 착용 펫들의 수익을 합산한다. 펫 화면에 개별·합계 수익을 표시한다. 중간에 착용을 바꾸면 실제 착용 시간만큼 적립하고 지급 주기와 미지급 적립량을 저장한다. 미착용 펫과 중복 보유 수량은 수익에 더하지 않는다. 오프라인·일시정지·사망 선택 중에는 적립하지 않는다.

### 추가 개정: BGM·보스 분노 연출

- 최신 연출 강화: 5초 각성 카운트에 경고음을 넣고 마지막 2초에는 붉은 외곽선·몸 떨림·화면 가장자리 예고를 더한다. 각성 시 0.85초 바닥 충격파, 짧은 화면 테두리 충격, 합성 포효·기존 설정 기반 진동을 재생한다. 추격 중 붉은 눈·두꺼운 외곽선·전방으로 기울인 질주·먼지 꼬리·느낌표 3개를 표시한다. 추격 경고는 기존 고정 알림 영역에서 지속되며 접촉 범위까지 남은 거리 5m 미만이면 ‘바로 뒤에 있어요!’로 전환한다. 가까워질수록 가장자리 경고 강도·BGM 박자·발소리 빈도를 높인다. 카메라·HUD 배치와 기존 속도·피해·5초 대기는 유지한다. 효과는 공유 InstancedMesh로 그리며 저화질에서 먼지 수를 줄인다. 동작 줄이기에서는 충격파·먼지·떨림·맥동·알림 확대 애니메이션을 제거한다. 자동 검증은 실행하지 않는다.

- 최신 조정: 1단계 속도는 유지하고 2~20단계 및 최종 보스 속도는 아래 기존 값의 1.5배로 적용한다. 모든 보스는 알 습득 후 5초 대기하며 5→1초 카운트다운을 표시한다. 대기 중 이동·공격은 없고 종료 시 각성 효과음·분노 연출과 추격을 시작한다. 알을 내려놓으면 대기를 취소하며 재습득하면 다시 5초 대기한다. 저장 시 남은 대기를 보존한다.

- 초반 추격 난이도 조정: 1~9스테이지 보스 속도는 각각 0.75/0.95/1.15/1.4/1.7/2.1/2.6/3.1/3.7로 낮춘다. 기본 플레이어 속도 1.6과 알 운반 감속을 고려해 초반 탈출 여유를 제공한다. 10~20스테이지는 기존 2.3+스테이지×0.2를 유지한다. 접촉 즉시 밀치기는 유지하며 밀치기 거리도 조정된 보스 속도에 비례한다.

- 평상시에는 잔잔한 합성 BGM, 알을 든 플레이어를 보스가 추격하면 빠른 박자의 긴박한 BGM을 재생한다. 기존 사운드 설정으로 BGM과 효과음을 함께 제어하며 사용자 입력 이후 재생한다. 설정·광고·사망 등의 모달에서는 BGM을 멈춘다.
- 추격 중 보스는 기본 크기의 1.5배로 커지고 빨간 테두리가 생긴다. 최종 보스도 기존 3배 크기에 1.5배를 곱한다. 접촉 범위도 확대 크기에 맞춘다. 알 낙하·회수·귀환·밤 초기화로 추격이 끝나면 원래 크기로 돌아간다.
- 추격 시작 시 ‘보스가 화났어요’를 3초 표시하고 추격 동안 화면 가장자리에 붉은 경고 맥동을 표시한다. 고정 오버레이를 사용해 해상도·카메라·HUD 배치를 바꾸지 않는다. 동작 줄이기 설정에서는 맥동 없이 고정 경고를 표시한다.

### 추가 개정: 개별 효과음·5초 부활 선택

- 배트 스윙, 알 두드리기/습득/낙하/회수/보관, 펫 부화, 보스 각성, 피격, 사망, 부활, 지역 진입, 밤 전환, 강화, 저체력 심박에 서로 다른 합성 효과음을 사용한다. 환경 위험은 물·불·기계·마법 계열 소리와 지역별 음정으로 구분한다. 기존 효과음 설정과 브라우저 사용자 입력 후 오디오 재생 규칙을 따른다.
- HP0 즉시 귀환 대신 사망 위치에서 5초 선택 창을 표시한다. ‘가상광고 보고 부활’ 또는 ‘그냥 복귀’를 선택하며, 5초 무선택 시 기지로 귀환하고 대기 원정 XP 70%를 정산한다. 알은 사망 위치에 떨어진다.
- 5초 안에 광고를 선택하면 선택 제한을 멈추고 10초 가상광고를 보여준다. 시청 완료 후 닫기 버튼으로 제자리 HP 완전 회복·3초 피해 무적 부활한다. 이미 떨어뜨린 알을 자동 지급하지 않는다. 조기 닫기·중복 보상은 허용하지 않는다.
- 사망 선택과 광고 중에는 탐험 시뮬레이션을 멈추지만 공통 낮밤 시간은 계속 흐른다. 밤이 오면 선택/광고를 종료하고 밤 귀환·필드 초기화를 적용한다. 재접속으로 사망 선택 5초를 새로 받지 않으며, 중단한 광고를 무료 부활로 처리하지 않는다.
- 이는 사용자 요청으로 이전 ‘광고 부활 비활성화’ 규칙을 대체한다. 실제 광고 SDK·실제 수익화는 사용하지 않는다. 자동 검증 없이 작업 후 커밋·푸시한다.

### 추가 개정: 길이 3배·다양한 지형·단계별 난이도

- 각 스테이지는 같은 길이의 1/2/3스텝으로 나눈다. 일반 스텝 길이 32, 마지막 스테이지 스텝 길이 150이다. 피해 배율은 1/1.25/1.5, 장애물 주기 배율은 1/0.85/0.7, 장식 밀도는 1/1.2/1.4다. 초반은 밝게, 중반은 기본 색, 후반은 짙게 전환하고 경계 기둥·바닥 무늬 및 기존 스테이지 표시의 STEP n/3으로 구분한다. 별도 반복 배너나 화면 확대는 추가하지 않는다.
- 20스테이지 3스텝 끝에는 기존 지역 보스와 별개로 크기 3배의 최종 보스 ‘창조의 수호자’를 추가한다. 끝벽 12 앞에 보스, 23 앞에 단일 둥지를 배치한다. 둥지에는 무작위 외형의 스페셜 알 1개만 생성한다. 등급은 SS/SSS/Secret 중 기존 비율을 정규화한 70/26/4%이며 일반 알의 낮아진 확률은 그대로다. 기존 20스테이지 둥지 5개는 유지해 전용 펫 10종을 모두 얻을 수 있다.
- 최종 보스도 알 습득 시 추격, 접촉 즉시 밀치기·낙하, 자기 스테이지 내 회수, 스테이지 밖 반출 시 회수 제외 규칙을 따른다. 접촉 범위는 몸 크기에 맞춰 3배이며 피해는 20스테이지 3스텝 기준이다. 밤에는 최종 보스와 스페셜 알도 함께 초기화한다. 재접속만으로 스페셜 알이 다시 생성되거나 등급을 다시 뽑지 않는다.

- 일반 스테이지 길이는 32→96, 마지막 스테이지는 150→450이다. 입구 위치는 유지하고 각 지역의 둥지·보스·위험 요소·끝벽을 새 길이에 맞춘다. routeVersion 1 저장의 원정 좌표·필드 알·둥지·보스는 입구 이후 거리를 3배로 옮겨 routeVersion 2로 보존한다. 거리 XP 기록도 함께 변환해 확장만으로 XP를 중복 지급하지 않는다.
- 기본 이동속도는 3.2→1.6이다. 권장 스피드는 1단계 3.2부터 다음 스테이지마다 정확히 2배(3.2, 6.4, 12.8, 25.6…)로 증가한다. 기존 강화·운동·펫 배수는 유지한다.
- 보스 피해는 40 + (스테이지−1)×20이다. 피해가 있는 환경 장애물에도 같은 최소 기본 피해를 적용하며 기존 추가 비례 피해·방어·1초 무적은 유지한다. 무피해 밀바람·지연·석화·거미줄은 무피해다. 1회 피해 상한은 최대 체력의 90%→100%로 바꿔 고단계 공격에 즉시 귀환할 수 있다.
- 각 스테이지에는 기존 테마 조형 3종과 아치·성장 기둥·등불·수반·운반 수레·토템·작은 석상·항아리·작은 다리·깃발·정자·나선 장치 12종을 더한 15종을 배치한다. 지역별 소재·식생·기계 구조·전용 조형 장식을 결합하고 좌우 비대칭 군집, 크기, 0~360도 회전과 기울기를 다양화한다.
- 좌우에는 이동 경계 바깥에서 이어지는 틈 없는 지형 벽을 두고 불규칙한 단차·식생·산호·건물·얼음 등 테마 장식으로 연결한다. 이동과 밀치기는 벽 안쪽으로 제한한다. 확장된 지형은 가까운 구간만 인스턴스 렌더링한다.
- 스테이지별 고정 위험 요소 수는 1~4단계 1개, 5~8단계 2개, 9~12단계 3개, 13~16단계 4개, 17~20단계 5개다. 각 위치는 독립 주기로 작동하며 동일 위험 요소를 중복 생성하지 않는다. 지형에 분산 배치하고 위험 표시로 머리 위 !를 유지한다. 위험 목록은 src/stage-data.ts의 STAGE_ENVIRONMENT_IDS를 따른다.
- 이번 밸런스·맵 변경에 자동 QA·빌드·스크린샷 검증을 실행하지 않는다. 앞서 수행한 빌드 성공은 배포 워크플로 수정에 대한 결과다.

### 추가 개정: 스테이지 전용 펫·희귀 확률·원형 둥지

- 20개 스테이지 각각 서로 겹치지 않는 전용 펫 10종, 총 신규 200종을 사용한다. C/B/A 각 2종, S/SS/SSS/Secret 각 1종이며, 알의 출처 stageId와 등급에 맞는 펫만 동일 확률로 부화한다. 도감은 1~20 스테이지별 카테고리와 각 10종 수집·완성 보상을 제공한다.
- 기존 펫 ID 0~99의 이름·외형·보유·장착·발견·보상 기록을 그대로 유지한다. 이전 100종은 별도 이전 도감에 표시하고 신규 ID 100~299를 뒤에 추가한다. 출처 없는 구 저장 알은 기존 부화 목록을 유지한다. 신규 200종 전체 보상과 기존 100종 보상은 별도 저장한다.
- 등급 확률은 C 47.5%, B 28%, A 15%, S 7%, SS 1.75%, SSS 0.65%, Secret 0.10%다. SS 이상 확률은 이전의 절반이며 감소분은 C에 더한다. 기존 알과 이미 수집한 펫의 등급은 바꾸지 않는다.
- 펫은 지역 장식·종별 몸체·뿔·지느러미·날개 등의 복셀 구조와 전용 이름·설명을 가진다. 크기는 10³~100³이며 큰 모델은 AIvoxel 50³ 이하 부분 생성 후 합성한다. 신규 도감 아이콘은 동일 설계의 등각 SVG를 사용한다.
- 둥지는 20³ 원형 그릇 형태로 가운데를 낮추고 가장자리에 짚·잔가지를 불규칙하게 엮는다. 사각 테두리를 제거하며 기존 알의 배치와 상호작용은 유지한다.
- 자동 QA·빌드·스크린샷 검증은 사용자 요청에 따라 실행하지 않는다.

이 절은 아래의 밤 탐험 허용, 반복 배너, 공격 버튼 제거, 보스 특수 공격 규칙보다 우선한다.

- 알 들기/내려놓기와 운동/판매 상황 행동을 우선한다. 기본 상태에서는 같은 버튼이 배트 스윙을 재생한다.
- 180초 공통 주기의 밤 45초 동안 탐험 입구를 물리적으로 막는다. 밤 시작 시 운반 알과 떨어진 필드 알을 잃고 농장으로 자동 귀환하며 대기 XP 70%를 정산한다. 보관함 알과 수집 펫은 유지한다. 재접속으로 귀환·정산을 회피하거나 중복 지급받지 못한다. 농장 안 이동은 허용하며 아침에 입구를 연다.
- 탐험 HUD는 스피드·낮밤 시계·상황 행동과 머리 위 체력 바를 중심으로 구성한다. 중복 권장 수치·XP 상세·귀환 위험도·추격 안내·상단 공격 예고는 숨긴다. HUD 표시 변화는 카메라 구도나 렌더 해상도에 영향을 주지 않는다.
- 지역 배너는 저장 데이터 기준 각 지역 최초 방문 때만 3초 표시한다. 역방향 재방문과 재접속에서는 반복하지 않는다.
- 보스 추격 속도는 2.3 + 단계×0.2다. 모든 보스는 접촉 범위 2 이내에 닿자마자 준비 동작 없이 밀쳐 알을 떨어뜨린다. 밀치기 거리는 보스 속도×2.4(1단계 6, 20단계 15.12)이며 벽에서 제한된다. 피해 무적 중에도 접촉 밀치기와 알 낙하는 적용하되 추가 HP 피해는 주지 않는다. 기존 특수 공격을 보스가 발사하지 않는다.
- 보스는 자기 스테이지 안에 떨어진 알을 직접 주워 원래 둥지로 운반한다. 회수 중인 알도 다시 집을 수 있으며 즉시 재추격한다. 한 번 스테이지 밖으로 빼낸 알은 그 낮 동안 회수 대상에서 제외한다. 밤에는 운반·낙하·회수 상태와 필드 알·보스를 모두 초기화하며 보관함 알과 수집 펫은 유지한다.
- 특수 공격은 정해진 지형 위치의 독립 장애물이다. 횡단 장애물은 정해진 길을 지나고, 다른 장치는 맵의 고정 위치에서 작동한다. 플레이어를 겨냥해 생성하거나 추적하지 않는다. 바닥 공격로와 상단 카운트다운 대신 장애물 위에 !를 표시한다.
- 스피드 성장 수치를 실제 이동 거리에 반영하고, 강화 전후 동일 시간 이동·운반 감속을 검증한다.

## 추가 개정: 지역별 알·밤·펫 행렬 (2026-09-23)

- 20개 지역마다 독립된 20³ 알 조형 5개씩, 총 100개 외형을 사용한다. 색뿐 아니라 실루엣·장식 구조가 다르며 운반·보관·부화에도 외형을 유지한다. 기존 35알/100펫 보상 ID는 유지한다.
- 사용자 스테이지 선택 UI를 없앤다. 농장부터 1→20으로 이동하며 기존 바로가기 저장 위치는 연결 맵의 같은 지역 위치로 옮긴다. 개발 QA의 지역 fixture만 유지한다.
- 180초 주기 중 밤을 45초로 늘리고 실제 환경광·태양광을 부드럽게 낮춘다. 달·별과 낮밤 남은 시간으로 표시한다. 밤에도 탐험/알 회수가 가능하며 같은 밤 재접속으로 둥지를 다시 섞지 않는다.
- 펫은 플레이어 이동 경로를 한 줄로 따라간다. 좌우 분산을 없애고 크기에 맞는 간격, 경로 보간, 부드러운 방향 회전을 적용한다.
- `appearance-e2e`로 100외형 배치, 펫 직진/회전, 낮밤 표시, 선택 UI 제거를 검증한다. 모바일 캡처는 직접 확인하고 사용자 요청대로 기준 이미지 비교는 생략한다.


## 최신 사용자 개정: 연결된 20지역·스피드 성장·잠든 보스 (2026-09-23)

이 절은 아래의 독립 차원 반복 맵, 원정 제한시간, 밤 강제 귀환, 일반 피격 알 유지, 초반 보스 무피해 규칙보다 우선한다.

- 농장에서 앞으로 걸으면 1→20지역이 연결되고, 귀환할 때는 역순으로 통과한다. 사용자 차원 선택은 제거하며 기존 선택 저장은 연결 맵 위치로 변환한다. 일반 지역 길이는 32, 마지막 지역은 150으로 세 구역을 유지한다.
- 20번 지역 끝은 황금 장식의 닫힌 돌벽으로 막는다. 플레이어 이동·넉백 모두 벽 앞에서 멈추며, 지역 바로가기 출발에서도 같은 제한을 적용한다.
- 지역 진입 시 번호·이름·권장 스피드를 3초간 표시한다. 배너는 입력을 막지 않고 공격 예고와 겹치지 않는다. 역방향 진입에도 표시한다.
- 초기 기본 스피드 4.5→3.2. 원정 시간 만료로 실패하지 않으며, 밤은 색 변화·미수집 둥지 갱신만 담당한다. 운반/낙하 알을 빼앗거나 강제 귀환시키지 않고 입구도 막지 않는다.
- 스피드 강화·운동·트레일·펫으로 더 먼 지역을 편하게 탐험한다. 권장 스피드는 이동 거리(둥지까지 약 20초 목표)와 보스 추격 속도/대표 운반 속도 65%에 여유 0.5를 더한 값 중 큰 값이며 최소 3.2다. 시간 제한이나 입장 잠금 수치가 아니다. 원정 시간 강화·시간 특성의 신규 선택 UI는 숨기되 기존 소유값을 삭제하지 않는다.
- 지역마다 수호자 1명과 둥지 알 5개. 수호자는 눈을 감고 Z 표시로 잠들어 있다가 자기 알을 훔치면 분노 표시와 함께 추격한다. 알을 내려놓거나 맞아 놓치면 둥지로 돌아가 다시 잠든다. 재습득하면 다시 깨어난다. 12 거리 밖에서는 원격 공격하지 않는다.
- 보스는 잠든 동안 공격하지 않는다. 환경 장애물은 별도로 유지한다. 초반 보스도 공격 예고 뒤 10~12 피해를 주며, 초반 물살·증기 같은 환경 효과는 무피해를 유지한다.
- 직접 피격은 피해·1초 무적·0.28초 동안 2.4 넉백·알 낙하로 처리한다. 떨어진 알은 해당 위치에 1개만 남고 다시 주울 수 있다. 지속 독 피해는 넉백/낙하를 반복하지 않는다. HP0은 기존 자동 귀환과 70% XP 정산을 유지한다.
- 알의 카탈로그 region, 출처 stageId, 수호자 인덱스를 분리한다. 귀환 보상과 완료 기록은 알의 출처 지역에 적용한다. 기존 35알·100펫·구매·도감 ID를 보존한다. 구 저장의 임시 필드 둥지는 연결 맵으로 재생성하고 운반 알은 보존한다.

검증: `qa:fast`, 이동 기반 `connected-e2e`, 보스 수면/추격/낙하/재습득 `progression-e2e`, 기존 E2E, 42패턴 캡처, 20지역 렌더·GPU 검사, 빌드·성능·프로덕션 E2E. 사용자의 ‘이미지 비교 패스’ 요청으로 이번 실행만 `QA_SKIP_IMAGE_COMPARE=1`을 사용한다. 기본 비교 검사는 변경하지 않는다.


> 최신 체력 UI 수정: 상단 체력 패널 대신 플레이어 머리 위에 작은 체력 바를 표시한다. HP가 최대치이면 숨기고 피해를 받으면 표시하며, 기지 귀환·레벨업으로 완전 회복하면 즉시 숨긴다. 카메라 이동을 따라가고 터치 입력을 막지 않는다. 50% 이하 노랑, 30% 이하 빨강·심장 아이콘·화면 가장자리 피드백을 유지한다. 남은 시간·귀환 위험도는 기존 HUD에 유지한다.

> 문서 상태: MVP v0.1  
> 플랫폼: **앱인토스 게임 미니앱 전용 WebView 앱**  
> 기준 화면: 세로 1080 × 1920 (9:16)  
> 렌더링: Three.js 기반 쿼터뷰 3D  
> 그래픽: 저해상도 복셀 캐릭터 및 오브젝트  
> 조작: 원핸드 가상 조이스틱 + 상황 행동 버튼 1개  
> 작업명: **알콩 원정대** (추후 변경 가능)

## 최신 개정: 체력·경험치·환경 회피 (2026-09-23)

이 절은 아래의 이전 피격 알 낙하/필드 회복/광고 부활/공격 버튼/스테이지 순서보다 우선한다. 게임의 중심은 전투가 아니라 알 운반과 귀환이다. 왼쪽 조이스틱, 오른쪽 들고가기/내려놓기만으로 탐험한다. 공격·회피 버튼을 두지 않으며 기존 멀티플레이 이동 기능은 보존하되 PvP 공격 진입점은 비활성화한다.

### 체력과 실패

HP 100으로 시작. 필드 자연 회복 없음. 기지 및 레벨업에서 전량 회복. 피격은 무적 확인 → 피해 → 약한 넉백 → 0.5초 20% 둔화 → 1초 무적 순서이며 같은 시각 충돌은 한 번만 처리한다. 일반 피격은 알을 유지한다. HP 0이면 알이 날아가고 바람/별/복셀 조각 연출 후 기지로 자동 귀환한다. 광고 부활 선택은 이 흐름에 개입하지 않는다. 단일 공격은 최대 HP의 90%를 상한으로 해 만피 즉사를 막으며, 낮은 현재 HP에서는 실패할 수 있다. 모든 방어 중첩의 피해 감소 상한은 50%다.

최대 HP = 100 + 5×(level−1) + 20×floor((level−1)/10). 기존 유료 체력 강화는 삭제하지 않고 추가 보너스로 보존하며 특성 +10도 더한다. 레벨 속도 배율 = min(1.6, 1+0.012×(level−1)). 기존 운동·트레일·강화는 소유 효과를 유지한다. 레벨 배율은 기본 속도에 적용하고 운반/상태/펫 배율을 순서대로 곱한다. 160% 상한은 레벨이 주는 배율의 상한이다.

상단 탐험 HUD: HP/바, 남은 시간, 귀환 위험도, 작은 LV/XP. HP >50% 정상, 31~50% 주의, 1~30% 위험. 색과 함께 ♥/!/텍스트, 피격 흔들림, 저체력 테두리·심박음, 햅틱을 사용한다. 조작 버튼/공격 예고는 공허에서도 숨기지 않는다.

### XP 저장·정산

다음 필요 XP = round(100×level^1.35). 현재 레벨/잔여 XP/다음 필요 XP/HP/최대 HP/최초 발견·부화 ID/최고 탐험 거리/원정 대기 XP를 저장한다. 필요 XP와 최대 HP는 로드 시 중앙 공식으로 재검산한다. 초과 XP는 여러 레벨을 연속 처리하며 레벨업마다 회복·연출·햅틱 후 저장한다.

| 원인 | XP |
|---|---:|
| C/B 알 귀환 | 20 |
| A/S/SS/SSS/SECRET 귀환 | 50/120/300/600/1000 |
| 새로운 알 종류 최초 근접 발견 | 30 |
| 새로운 펫 종류 최초 부화 | 100 |
| 최고 탐험 거리 갱신 | 10 단위마다 2 |
| 원정 실패 | 해당 원정 대기 XP의 70%, 내림 |

귀환 XP는 실제 보관 성공 시에만 발생한다. 발견/거리 XP는 원정 중 대기하며 귀환 시 100%, HP·시간·밤 강제 귀환 시 70%를 정산한다. 최초 기록은 대기 XP와 함께 저장하므로 실패/재시작으로 반복 획득하지 못한다. 기지 부화 XP는 즉시 지급한다. 기존 발견/보유 기록은 기지급으로 이관해 소급 중복 보상을 막는다.

### 특성

5레벨마다 1포인트, 기지에서 선택. 튼튼한 탐험가(+10 HP, 최대5), 가벼운 발걸음(+2% 속도, 최대5), 운반 전문가(무게 페널티 3% 감소, 최대5), 위기 탈출(HP≤30%에서 +10% 속도, 최대1), 보호 장비(원정 첫 피해 30% 감소, 최대1), 시간 감각(+2초, 최대5). 효과/중첩 한도는 데이터에서 관리한다.

### 스테이지 및 저장 호환

기지의 차원 선택에서 1~20을 선택한다. 낮은 레벨에도 입장을 막지 않고 권장 레벨과 대표 공격의 예상 피격 허용 횟수를 보여준다. 기존 35알/100펫 카탈로그 ID는 보존하고 스테이지 ID를 별도로 저장한다. 차원은 공통 농장에서 출발하는 독립 탐험 구역이며 선택은 기지에서만 가능하다. 4단계 알 귀환 성공을 완료로 정의하고 체력 해금 안내를 한 번 보여준다. 이를 건너뛰고 5 이상에 들어가도 경고와 함께 체력을 활성화해 위험을 우회하지 못하게 한다.

| 단계 | 세계 | 환경 패턴 |
|---|---|---|
|1|몽글 초원|움직이는 건초, 무피해 밀침|
|2|장난감 왕국|경고 선로의 태엽 기차, 무피해 밀침|
|3|얕은 산호 바다|물살 밀침, 문어 먹물 시야 방해, 무피해|
|4|화산 입구|붉은 바닥 후 증기 도약, 무피해|
|5|크라켄 심해|촉수 내려찍기 25/1.5초, 가로 휩쓸기 20/2초, 산호 엄폐|
|6|유령 학교|사물함 부채꼴 20/1.2초, 책 직선/1초, 책상 엄폐|
|7|네온 사이버시티|순차 레이저 18/1초, 드론 25/1.5초·마지막0.3초 고정|
|8|황금 사막왕국|전갈 25/1.3초·2초10%둔화, 무피해 모래바람|
|9|태초의 공룡섬|발구르기35/2초·3회 예고, 랩터 직선/1초·바위 차단|
|10|도깨비 달마을|도깨비불3개 각10/1.5초 추적·장애물 소멸, 방망이30·별가루 회수|
|11|올림포스 신전|번개30/1.4초·3회 점멸, 시선 석화 게이지·1.5초 제한|
|12|외계인51구역|UFO 견인 중심20, 포탑1초 예고 후 느린 추적탄|
|13|스팀펑크 공중도시|증기20/1.2초 강한 밀침, 경로 톱니15|
|14|영원의 빙하|고드름25/1.3초, 머무르면 깨지는 얼음20·운반 가속|
|15|뒤죽박죽 꿈나라|악몽손20/1.5초·입력 탈출, 작은 구역의0.5초 입력 지연|
|16|방사능 폐허|덩굴20/1.2초, 웅덩이 초당5·이탈 즉시 중단|
|17|곤충 소인국|사마귀30/1.4초 부채꼴, 거미줄 무피해·2초40%둔화|
|18|고철 로봇행성|금속 알 강화 견인 중심20, 압축기35/2초·탈출 방향|
|19|은하수 천문대|순차 유성 각15, 태양풍2초·구조물 엄폐|
|20|공허와 창조주의 정원|공허 손25/사라지는 발판 → 기억 패턴 최대2종 → 황금 파동 최대HP20%|

권장 레벨은 1~4:1~10, 5~8:11~22, 9~12:23~34, 13~16:35~46, 17~19:47~57, 20:60. 고정 피해 수치와 권장 피격 횟수에는 일부 불일치가 있다(예: HP495에 15 피해는 약33회). 명시된 기본 피해를 보존하고 후반 3~8% 비례 피해를 더하며 실제 허용 횟수를 UI와 QA 보고서에 표시한다. 수치를 몰래 키워 목표 횟수에 맞추지 않는다.

### 위험 관리자·회피 계약

정의 데이터는 id/displayName/stageId/damage/damagePercent/telegraphDuration/activeDuration/cooldown/knockback/slowMultiplier/slowDuration/shape/targetingType/carryTelegraphBonus를 가진다. Idle→Telegraph→Active→Recovery→Cooldown, 고정 시간 간격 판정으로 FPS 차이를 제한한다. 시각 오브젝트와 판정은 분리한다. 일반 바닥≥1.2초, 직선≥1초, 추적≥1.5초, 넓은 공격≥2초, 잡기≥1.3초. 운반 시 예고20% 연장. 지형 위험도 예고하며 공격은 카메라 근처에서 생성하고 화면 가장자리 경고를 제공한다. 최종 SECRET 운반 시 이미 학습한 패턴의 빈도만 조금 증가한다.

첫 구현은 크라켄 2패턴/엄폐/운반 회피를 완성해 검증한 뒤 6~20으로 확장한다. 효과는 재사용 풀로 제한하며 별도의 공격·회피 입력을 추가하지 않는다. 시간 지연/석화도 사전 예고 범위를 벗어나면 피할 수 있어야 한다.

### 검증 계약

HP 상한/무적/단일 실패/기지 회복/알 유실, 행동별 XP/최초 중복/연속 레벨/저장/실패70%, 예고 무피해/범위/엄폐/운반 연장/30·60·120FPS/같은 공격 중복을 단위 테스트한다. E2E는 4완료→해금→5촉수 예고→이동 회피→피격→운반 귀환·레벨업→재원정HP0·유실·귀환. 360px 포함 모바일 스크린샷을 직접 검토하고 성능 예산·프로덕션 빌드를 확인한다. 실제 토스 햅틱/수명주기/성능은 실기기 항목으로 분리한다.

---

## 0. Codex 작업 지시

이 문서를 프로젝트의 단일 기준 명세서로 사용한다.

구현 시 다음 원칙을 반드시 지킨다.

1. 모바일 세로 화면을 최우선으로 구현한다.
2. 마우스보다 터치 조작을 기준으로 설계한다.
3. 캐릭터 이동은 화면 좌측 하단의 가상 조이스틱으로 처리한다.
4. 상호작용은 화면 우측 하단의 상황 행동 버튼 하나로 처리한다.
5. 탐험 중 플레이어가 알의 상호작용 범위에 들어가면 행동 버튼을 `들고가기`로 변경한다.
6. 알을 든 상태에서 기지 귀환 지점에 들어가면 자동으로 알을 보관한다.
7. 제한시간 또는 귀환 가능 시간을 넘기면 운반 중인 알이 날아가며 획득에 실패한다.
8. 부화실에서는 수동 터치 피해, 초당 자동 타격, 초당 타격 횟수 강화를 모두 지원한다.
9. 기본 복셀 모델의 제작 격자는 20 × 20 × 20이다.
10. 희귀·거대 개체만 최대 50 × 50 × 50 격자를 허용한다.
11. 복셀 하나마다 Mesh를 생성하지 않는다. 병합 지오메트리, InstancedMesh 또는 최적화된 복셀 로더를 사용한다.
12. MVP에서는 실시간 멀티플레이를 구현하지 않는다. 서버 기능은 교체 가능한 인터페이스로 분리한다.
13. 앱인토스 WebView SDK와 `@apps-in-toss/web-framework`를 기준으로 구현한다.
14. 앱인토스 게임 로그인·리더보드·네이티브 저장소·Safe Area·행동 분석은 출시 필수 범위다.
15. 앱인토스 광고·공유 리워드·인앱 결제·토스 포인트 프로모션은 출시 직전 수익화 범위다.
16. 일반 브라우저 실행은 로컬 개발과 디버깅을 위한 폴백일 뿐, 정식 배포 환경이 아니다.
17. iframe은 사용하지 않는다. 게임과 Three.js 캔버스는 미니앱 번들 안에서 직접 실행한다.

---

## 1. 게임 개요

### 1.1 한 줄 소개

더 멀리 탐험해 신비한 알을 발견하고, 제한시간 안에 기지로 가져와 귀여운 복셀 생명체를 부화시키는 수집·방치형 원정 게임.

### 1.2 핵심 판타지

플레이어는 작은 탐험가 `알콩이`가 되어 미지의 섬을 탐험한다. 먼 지역일수록 희귀한 알이 등장하지만, 능력보다 멀리 욕심내어 이동하면 제한시간 안에 돌아오지 못하고 알을 놓치게 된다. 안전하게 가져온 알은 부화실에서 직접 두드리거나 자동화 장비를 사용해 깨며, 알에서는 `몽글이`라는 복셀 생명체가 태어난다.

### 1.3 핵심 재미

- 멀리 갈수록 좋은 알이 나온다는 기대감
- 귀환 시간을 계산하며 욕심과 안전 사이에서 선택하는 긴장감
- 희귀한 알을 들고 기지까지 돌아오는 성취감
- 알의 균열이 점점 커지는 시각적 피드백
- 무엇이 나올지 모르는 부화 연출
- 태어난 몽글이를 수집하고 전시하는 소유욕
- 이동속도·부화력·자동화가 눈에 띄게 증가하는 성장감

### 1.4 주요 레퍼런스에서 가져올 구조

- 알 탐색 → 선택 → 운반 → 귀환 구조
- 낮은 성장 단계에서는 먼 지역의 알을 가져오기 어려운 위험·보상 구조
- 제한시간이 끝나면 알을 잃는 명확한 실패
- 이동속도 및 운반 능력 강화
- 알 등급에 따른 크기·무게·희귀도 차이
- 부화 게임의 알 외형, 균열 단계, 도감, 희귀 생명체 수집

다른 게임의 캐릭터, 명칭, UI, 맵, 사운드, 정확한 수치와 시각 표현은 복제하지 않는다.

---

## 2. 플랫폼 및 기술 목표

### 2.1 대상 환경

- 앱인토스 게임 카테고리 미니앱
- 앱인토스 WebView SDK 및 Granite 런타임
- Android/iOS 토스 앱 내부 WebView
- 일반 모바일 브라우저는 개발용 폴백만 지원
- 세로 화면 전용
- 기준 디자인 해상도: 1080 × 1920
- 최소 대응 비율: 9:19.5
- 터치 우선, 개발 편의를 위한 키보드 입력 보조 지원

### 2.2 권장 기술 스택

- TypeScript
- Vite
- Three.js
- React 또는 바닐라 TypeScript 기반 HTML/CSS HUD
- `@apps-in-toss/web-framework`
- Zustand 또는 가벼운 자체 상태 저장소
- Howler.js 또는 Web Audio API
- Vitest
- Playwright 모바일 뷰포트 테스트

React를 사용한다면 Three.js 장면은 `@react-three/fiber`를 사용할 수 있다. 빠른 MVP가 목표라면 순수 Three.js와 HTML HUD 조합도 허용한다.

### 2.4 앱인토스 구성 원칙

- 앱인토스 공식 샘플 프로젝트와 현재 개발자 문서를 먼저 확인한 뒤 패키지 버전을 결정한다.
- 앱 이름, 스킴, 번들 설정은 앱인토스 콘솔에 등록한 값과 일치시킨다.
- 게임 화면 내부에 iframe을 삽입하지 않는다.
- 로그인 토큰 검증, 결제 승인 등 서버 처리가 필요한 기능은 클라이언트에 비밀키를 포함하지 않고 별도 서버 API로 처리한다.
- 게임센터 리더보드는 게임 카테고리 미니앱 승인 이후 연결한다.
- 리더보드가 열리면 미니앱이 백그라운드로 전환될 수 있으므로 즉시 게임을 일시정지하고 저장한다.
- 앱이 백그라운드로 이동하거나 보이지 않게 되면 탐험 타이머를 서버 시간 기준으로 정합성 있게 처리한다.
- 커스텀 UI에 앱인토스 가격이나 보상을 하드코딩하지 않는다.

### 2.3 렌더링 목표

- 일반 기기: 60 FPS 목표
- 저사양 기기: 최소 30 FPS
- 초기 로딩: 가능한 한 3초 이내
- 첫 플레이에 필요하지 않은 생물·맵 에셋은 지연 로딩
- 동시에 화면에 표시되는 활성 캐릭터 수 제한
- 그림자 품질 단계 제공
- `devicePixelRatio` 상한 1.5~2.0 적용

---

## 3. 화면 방향 및 카메라

### 3.1 쿼터뷰 구성

- 월드 좌표에서 Y축을 높이로 사용한다.
- 카메라는 플레이어의 뒤쪽 대각선 상공에서 내려다본다.
- 권장 초기 카메라 방향: 월드 기준 X/Z축에서 약 45도 회전, 지면 기준 35~50도 하향.
- 모바일에서 원근 왜곡이 심하지 않도록 OrthographicCamera를 우선 검토한다.
- 희귀 대형 알과 몽글이가 화면을 가리지 않도록 카메라 줌을 상황에 따라 부드럽게 조정한다.

### 3.2 카메라 행동

- 플레이어를 부드럽게 추적한다.
- 이동 방향 앞쪽을 화면에 조금 더 보여준다.
- 알을 들면 카메라를 5~10% 축소해 주변 장애물을 더 잘 보이게 한다.
- 부화 연출 중에는 별도의 부화 카메라 프리셋을 사용한다.
- 카메라 회전은 MVP에서 플레이어가 직접 조작하지 않는다.

### 3.3 안전 영역

- 상단 상태바와 하단 홈 인디케이터 영역을 침범하지 않는다.
- HUD는 CSS `env(safe-area-inset-*)` 또는 앱인토스 Safe Area 값을 사용한다.
- 실제 렌더링 해상도와 UI 논리 해상도를 분리한다.

---

## 4. 원핸드 조작

### 4.1 기본 배치

- 가상 조이스틱: 화면 좌측 하단
- 상황 행동 버튼: 화면 우측 하단
- 설정에서 좌우 반전 옵션 제공 예정
- 한 손 엄지로 이동과 행동을 번갈아 수행할 수 있는 범위에 배치

### 4.2 가상 조이스틱

- 고정형 또는 터치 시작 지점에 나타나는 반고정형
- 데드존 적용
- 입력 벡터 크기에 따라 걷기/달리기 속도 조절
- 손가락이 조이스틱 영역 밖으로 나가도 입력을 제한 범위로 정규화
- 멀티터치 시 조이스틱 포인터 ID를 유지해 다른 버튼 입력과 충돌하지 않게 한다.

### 4.3 상황 행동 버튼

상태에 따라 버튼 문구와 기능을 변경한다.

| 상황 | 버튼 문구 | 행동 |
|---|---|---|
| 상호작용 대상 없음 | 탐색 | 가까운 목표 방향 표시 |
| 알 근처, 빈손 | 들고가기 | 해당 알을 들어 올림 |
| 알 운반 중 | 내려놓기 | 현재 위치에 알을 내려놓음 |
| 기지 귀환 범위 | 보관하기 | 알을 부화 대기열로 이동 |
| 부화실 | 두드리기 | 선택한 알에 수동 피해 |
| 결과 화면 | 확인 | 결과 닫기 |

상호작용 가능한 알이 여러 개라면 플레이어 정면과 가장 가까운 알을 우선한다.

### 4.4 개발용 보조 입력

- WASD 또는 방향키: 이동
- E 또는 Space: 상황 행동
- Esc: 일시정지
- 디버그 모드에서만 카메라 조작 허용

---

## 5. 핵심 게임 루프

```text
기지에서 출발
→ 탐험 지역으로 이동
→ 알 발견
→ 접근 후 '들고가기'
→ 이동속도 감소 상태로 귀환
→ 기지에 알 보관
→ 수동/자동으로 알 깨기
→ 몽글이 부화
→ 몽글이 배치 및 생산
→ 재화로 이동·운반·부화 능력 강화
→ 더 먼 지역 탐험
```

### 5.1 짧은 세션 목표

- 탐험 1회: 30~90초
- 알 부화: 초기 5~30초, 중후반은 자동화 병행
- 첫 몽글이 획득: 플레이 시작 후 1분 이내
- 첫 귀환 실패: 3~6분 사이에 자연스럽게 경험하도록 설계
- 첫 희귀 몽글이: 5~10분 사이

---

## 6. 탐험 시스템

### 6.1 맵 구조

MVP에서는 하나의 길게 이어진 탐험 맵을 사용한다.

```text
[기지]
  ↓
[풀숲]
  ↓
[버섯숲]
  ↓
[수정동굴]
  ↓
[구름유적]
  ↓
[우주균열]
```

- 기지에서 멀어질수록 희귀 알의 가중치가 증가한다.
- 구역 경계는 색상, 지형, 식생, 음악으로 명확히 구분한다.
- 레벨이 낮아도 먼 구역 진입 자체는 가능하게 한다.
- 다만 제한시간, 속도, 알의 무게 때문에 현실적으로 귀환이 어렵게 만든다.

### 6.2 탐험 제한시간

- 탐험 시작 시 타이머가 작동한다.
- 기본 시간 예시: 45초
- 시간 강화와 몽글이 효과로 증가 가능
- 남은 시간 10초부터 UI와 사운드로 경고
- 남은 시간 5초부터 강풍 연출 시작

### 6.3 안전 거리

```text
safeDistance = moveSpeed * remainingTime * 0.5 * safetyFactor
```

- `0.5`: 왕복 이동을 고려한 값
- `safetyFactor`: 장애물과 조작 실수를 고려해 0.75~0.9 사용
- UI에 정확한 숫자를 강제 노출하기보다 귀환 가능성을 색으로 표현한다.

| 표시색 | 의미 |
|---|---|
| 초록 | 여유 있게 귀환 가능 |
| 노랑 | 위험하지만 가능 |
| 빨강 | 현재 능력으로 귀환이 매우 어려움 |

### 6.4 알 스폰

- 구역별 스폰 포인트 또는 탐색 가능한 작은 군집 사용
- 한 번에 너무 많은 알을 노출하지 않는다.
- 일반 알은 길 가까이에, 희귀 알은 우회로와 위험 지역에 배치한다.
- 희귀 알 출현 시 빛기둥, 고유 사운드, 진동을 사용한다.
- 일정 시간이 지나면 알이 날아가거나 땅속으로 사라져 새 알로 교체된다.

### 6.5 알 상호작용

- 상호작용 반경 진입 시 알 아래에 원형 하이라이트 표시
- 행동 버튼이 `들고가기`로 변경
- 버튼을 누르면 0.25~0.5초의 짧은 줍기 연출 후 머리 위 또는 등에 알을 든다.
- 알을 들고 있는 동안 다른 알은 들 수 없다.
- 향후 업그레이드로 운반 슬롯을 2개까지 늘릴 수 있지만 MVP 기본값은 1개다.

### 6.6 운반 페널티

```text
carryMoveSpeed = baseMoveSpeed * eggWeightMultiplier * carryUpgradeMultiplier
```

| 알 등급 | 기본 속도 배율 |
|---|---:|
| 일반 | 0.90 |
| 희귀 | 0.80 |
| 영웅 | 0.68 |
| 전설 | 0.55 |
| 미확인 | 0.42 |

알 크기와 무게는 연관되지만 완전히 동일할 필요는 없다. 작지만 밀도가 높은 특수 알도 만들 수 있다.

### 6.7 귀환 성공

- 기지의 귀환 원 안에 알을 든 상태로 들어오면 자동 보관한다.
- 0.5초 동안 알이 보관 장치로 빨려 들어가는 연출을 사용한다.
- 결과 패널에 알 이름, 등급, 발견 거리, 예상 부화 시간을 표시한다.
- 처음 발견한 알이면 도감에 실루엣을 등록한다.

### 6.8 귀환 실패

타이머가 0이 되면 다음 순서로 처리한다.

1. 화면 가장자리에 바람 효과 표시
2. 플레이어가 들고 있는 알이 흔들림
3. 알이 손에서 빠져나옴
4. 알에 작은 날개 또는 바람 궤적이 생김
5. 알이 탐험 지역 바깥으로 날아감
6. `알을 놓쳤어요! 속도를 강화하고 다시 도전해보세요.` 표시
7. 플레이어를 기지로 귀환시킴

MVP에서 실패한 알은 완전히 소실한다. 하루 1회 무료 구조권 또는 선택형 보상 광고는 추후 추가한다.

---

## 7. 부화 및 알 깨기

### 7.1 기본 개념

가져온 알은 부화 대기열에 저장된다. 플레이어는 알을 직접 터치해 껍질을 깨거나, 자동 장비와 몽글이를 배치해 초당 피해를 줄 수 있다.

### 7.2 알 내구도

```text
eggHP = baseHPByRegion * rarityMultiplier * sizeMultiplier
```

초기 예시:

| 알 | 내구도 |
|---|---:|
| 풀잎 알 | 30 |
| 물방울 알 | 120 |
| 버섯 알 | 400 |
| 수정 알 | 1,500 |
| 화석 알 | 7,500 |
| 우주 알 | 40,000 |
| 미확인 알 | 250,000 |

### 7.3 수동 타격

```text
manualDamagePerTap = baseTapDamage * toolMultiplier * temporaryBuff
```

- 알 또는 `두드리기` 버튼을 누르면 피해 적용
- 멀티터치 연타 악용을 막기 위해 최대 입력 빈도 설정
- 타격 시 알 흔들림, 작은 파편, 숫자 팝업, 햅틱 제공

### 7.4 자동 타격

```text
autoDPS = damagePerAutoHit * autoHitsPerSecond
totalDPS = autoDPS + companionDPS
```

자동 타격 아이템 예시:

| 아이템 | 타격 피해 | 초당 타격 |
|---|---:|---:|
| 병아리 부리 | 1 | 1 |
| 통통 망치 | 3 | 2 |
| 태엽 딱따구리 | 5 | 4 |
| 드릴 두더지 | 12 | 5 |
| 레이저 아홀로틀 | 30 | 7 |
| 별빛 파쇄기 | 100 | 10 |

### 7.5 강화 축

서로 다른 성장감을 위해 강화 항목을 분리한다.

1. **터치 부화력**: 한 번 누를 때 주는 피해 증가
2. **자동 타격 피해**: 자동 장비가 한 번에 주는 피해 증가
3. **초당 타격 횟수**: 자동 장비의 공격 빈도 증가
4. **동시 부화 슬롯**: 동시에 작업할 수 있는 알 증가
5. **균열 보너스**: 알 체력이 일정 비율 이하일 때 피해 증가

### 7.6 균열 단계

알 체력 비율에 따라 최소 4개의 상태를 표현한다.

- 100~76%: 온전함
- 75~51%: 작은 금
- 50~26%: 큰 균열
- 25~1%: 흔들림과 내부 빛
- 0%: 부화 연출

단순 텍스처 교체 또는 복셀 파편 오브젝트 추가 방식으로 구현한다.

### 7.7 오프라인 진행

MVP에서는 최대 2시간의 오프라인 자동 타격을 지원한다.

```text
offlineDamage = min(offlineSeconds, maxOfflineSeconds) * savedAutoDPS
```

부화가 완료되어도 다음 알로 자동 전환하지 않는다. 자동 전환은 별도 업그레이드로 확장 가능하다.

---

## 8. 몽글이 수집 시스템

### 8.1 디자인 공식

```text
몽글이 = 친숙한 동물 70% + 판타지 요소 20% + 이상한 특징 10%
```

예시:

- 물방울 꼬리를 가진 수달
- 등에 온천이 있는 카피바라
- 그림자처럼 녹아내리는 고양이
- 달 조각 모양 귀를 가진 토끼
- 보석 등껍질을 가진 두더지
- 몸속에 별이 회전하는 아기용

### 8.2 희귀도

| 내부 등급 | 세계관 명칭 | 기본 비중 |
|---|---|---:|
| Common | 야생종 | 60% |
| Rare | 변이종 | 25% |
| Epic | 환상종 | 10% |
| Legendary | 전설종 | 4% |
| Secret | 미확인종 | 1% |

실제 확률은 알 종류와 구역에 따라 다르게 설정한다.

### 8.3 몽글이 능력

몽글이는 생산 또는 탐험 보너스를 가진다.

| 역할 | 예시 효과 |
|---|---|
| 부화형 | 자동 DPS 증가 |
| 속도형 | 기본 이동속도 증가 |
| 운반형 | 알 무게 페널티 감소 |
| 시간형 | 탐험 제한시간 증가 |
| 행운형 | 희귀 알 발견 확률 증가 |
| 증폭형 | 인접 몽글이 효과 증가 |

### 8.4 중복 처리

- 동일 몽글이 3마리 합성 → 별 1단계 증가
- 별 등급 상승 시 능력치와 일부 외형 변화
- 중복 획득이 완전한 실패가 되지 않도록 도감 경험치 또는 별가루 지급

---

## 9. 복셀 아트 규격

### 9.1 기본 격자

- 일반 캐릭터: 최대 20 × 20 × 20 voxels
- 일반 알: 최대 16 × 20 × 16 voxels 권장
- 일반 환경 소품: 20 × 20 × 20 안에서 제작
- 희귀 캐릭터·보스·거대 알: 최대 50 × 50 × 50 voxels

`20 × 20 × 20`은 제작 격자 해상도이며 Three.js 월드에서의 실제 크기와 동일하지 않다.

### 9.2 월드 크기

권장 월드 단위:

| 대상 | 월드 높이 기준 |
|---|---:|
| 플레이어 알콩이 | 1.0~1.2 |
| 일반 몽글이 | 0.7~1.0 |
| 일반 알 | 0.6~0.9 |
| 영웅 몽글이 | 1.0~1.3 |
| 전설 몽글이 | 1.2~1.7 |
| 거대 미확인종 | 최대 2.5 |

50 복셀 모델이라도 화면에서 무조건 2.5배가 되는 것은 아니다. 세부 묘사를 늘리기 위해 높은 격자를 사용할 수 있다.

### 9.3 모델 제작 규칙

- 정면, 후면, 좌측, 우측 실루엣이 구분되어야 한다.
- 기본 캐릭터는 큰 머리와 작은 몸을 사용한다.
- 눈은 1~2복셀 단위로 단순화한다.
- 등급 차이를 색상 변경만으로 해결하지 않는다.
- 전설 이상은 뿔, 꼬리, 날개, 궤도 물체처럼 실루엣 요소를 추가한다.
- 바닥에 닿는 기준점과 모델 중심축을 통일한다.
- 팔·다리 등 애니메이션 파트가 필요하면 그룹을 분리한다.
- 모델마다 빈 복셀을 포함한 전체 격자 크기, 실제 점유 범위, 피벗을 메타데이터로 저장한다.

### 9.4 팔레트

- 일반 모델은 8~16색 이내
- 전설 모델은 효과색을 포함해 최대 24색 권장
- 과도한 그라데이션 금지
- 재질은 기본적으로 무광
- 하이라이트는 한 단계 밝은 복셀 면으로 표현
- 그림자는 실제 조명과 단순 색면을 함께 활용

### 9.5 포맷

권장 파이프라인:

```text
MagicaVoxel(.vox)
→ 변환 스크립트 또는 glTF Export
→ 최적화된 .glb
→ Three.js GLTFLoader
```

- 텍스처 필터는 nearest 사용
- 가능하면 동일 팔레트의 머티리얼 공유
- 정적 복셀은 면 병합 또는 greedy meshing 적용
- 하나의 복셀마다 독립 Mesh 생성 금지
- Draco 또는 Meshopt 압축 검토

### 9.6 애니메이션

MVP 애니메이션은 파트 단위 회전과 위치 이동으로 처리할 수 있다.

- Idle: 상하 2~4픽셀 상당의 호흡
- Walk: 몸통 바운스 + 발 교차
- Carry: 양팔 또는 머리 위에 알 고정
- Hit: 망치 또는 몸통이 알 방향으로 이동
- Hatch reaction: 점프 또는 회전
- Rare idle: 전용 파티클 또는 보조 물체 궤도

---

## 10. UI/UX — 1080 × 1920 기준

### 10.1 탐험 화면

```text
┌────────────────────────────┐
│ Lv.12   별가루 12.4K   설정 │
│ 남은 시간 00:32  [게이지]   │
│                            │
│                            │
│       Three.js 월드         │
│       쿼터뷰 탐험            │
│                            │
│          [희귀 알]           │
│                            │
│ [가상 조이스틱]   [들고가기] │
│      탐험 위험도: 노랑       │
└────────────────────────────┘
```

### 10.2 부화실 화면

```text
┌────────────────────────────┐
│ 부화실   자동 부화력 64/s    │
│                            │
│          [선택한 알]         │
│         HP 742 / 1500       │
│          큰 균열 상태        │
│                            │
│ 자동 장비와 몽글이 배치 영역 │
│                            │
│ [알 목록]         [두드리기] │
│ 강화  도감  탐험  연구소     │
└────────────────────────────┘
```

### 10.3 터치 크기

- 핵심 버튼 최소 96 × 96 디자인 픽셀 권장
- 행동 버튼은 160~220px 범위의 원형 또는 둥근 사각형
- 조이스틱 외곽은 약 240~300px
- 중요한 텍스트는 32px 이상을 기준으로 기기별 스케일링
- 하단 네비게이션과 시스템 홈 영역 사이 여백 확보

### 10.4 UI 우선순위

탐험 중 항상 보여야 하는 정보:

1. 남은 시간
2. 현재 운반 중인 알
3. 귀환 위험도
4. 이동 조작
5. 상황 행동

재화, 상점, 도감 알림은 탐험을 방해하지 않게 축소한다.

---

## 11. 성장 및 밸런스

### 11.1 주요 능력치

```ts
type PlayerStats = {
  expeditionLevel: number;
  moveSpeed: number;
  carrySpeedMultiplier: number;
  expeditionDurationSeconds: number;
  manualTapDamage: number;
  autoHitDamage: number;
  autoHitsPerSecond: number;
  hatchSlotCount: number;
  luckMultiplier: number;
};
```

### 11.2 초반 성장 의도

```text
빠른 성장
→ 첫 희귀 알을 보고 욕심냄
→ 귀환 실패
→ 속도 또는 시간 강화
→ 같은 거리 재도전
→ 성공
→ 다음 구역에서 다시 정체
```

### 11.3 업그레이드 가격 예시

```text
upgradeCost(level) = baseCost * growthRate ^ level
```

- 이동속도: growthRate 1.55~1.7
- 운반속도: growthRate 1.65~1.8
- 터치 피해: growthRate 1.45~1.6
- 자동 피해: growthRate 1.6~1.75
- 초당 타격 횟수: 단계 수를 제한하고 가격을 크게 증가

### 11.4 첫 10분 목표

| 시간 | 예상 사건 |
|---|---|
| 0~1분 | 첫 알 귀환 및 첫 몽글이 부화 |
| 1~3분 | 자동 타격 장비 해금 |
| 3~5분 | 먼 알에 도전하다 첫 실패 |
| 5~7분 | 속도 강화 후 재도전 성공 |
| 7~10분 | 희귀 몽글이 획득 및 두 번째 지역 진입 |

---

## 12. 데이터 모델

### 12.1 알 데이터

```ts
type EggDefinition = {
  id: string;
  displayName: string;
  regionId: string;
  rarity: 'common' | 'rare' | 'epic' | 'legendary' | 'secret';
  maxHP: number;
  weightMultiplier: number;
  visualScale: number;
  modelUrl: string;
  hatchPoolId: string;
  despawnSeconds: number;
};
```

### 12.2 몽글이 데이터

```ts
type MongleDefinition = {
  id: string;
  displayName: string;
  rarity: 'common' | 'rare' | 'epic' | 'legendary' | 'secret';
  modelUrl: string;
  voxelGrid: [number, number, number];
  worldScale: number;
  passiveType: 'hatchDps' | 'moveSpeed' | 'carrySpeed' | 'time' | 'luck' | 'amplify';
  passiveValue: number;
  flavorText: string;
};
```

### 12.3 저장 데이터

```ts
type SaveData = {
  version: number;
  currencies: {
    stardust: number;
    premium?: number;
  };
  stats: PlayerStats;
  eggInventory: EggInstance[];
  mongleInventory: MongleInstance[];
  activeMongleIds: string[];
  discoveredEggIds: string[];
  discoveredMongleIds: string[];
  lastSavedAt: number;
};
```

저장 데이터에는 반드시 스키마 버전을 포함하고 마이그레이션 함수를 둔다.

---

## 13. 코드 구조 제안

```text
src/
├─ app/
│  ├─ App.ts
│  ├─ GameState.ts
│  └─ Router.ts
├─ game/
│  ├─ core/
│  │  ├─ GameLoop.ts
│  │  ├─ InputManager.ts
│  │  ├─ SaveManager.ts
│  │  └─ EventBus.ts
│  ├─ exploration/
│  │  ├─ ExplorationScene.ts
│  │  ├─ PlayerController.ts
│  │  ├─ EggSpawner.ts
│  │  ├─ EggInteraction.ts
│  │  ├─ CarrySystem.ts
│  │  ├─ ExpeditionTimer.ts
│  │  └─ ReturnZone.ts
│  ├─ hatchery/
│  │  ├─ HatcheryScene.ts
│  │  ├─ HatchSystem.ts
│  │  ├─ AutoHitSystem.ts
│  │  └─ HatchAnimation.ts
│  ├─ collection/
│  │  ├─ CollectionSystem.ts
│  │  ├─ MongleSystem.ts
│  │  └─ FusionSystem.ts
│  └─ rendering/
│     ├─ SceneManager.ts
│     ├─ CameraRig.ts
│     ├─ VoxelModelLoader.ts
│     ├─ EffectsPool.ts
│     └─ QualityManager.ts
├─ ui/
│  ├─ VirtualJoystick.ts
│  ├─ ActionButton.ts
│  ├─ ExplorationHUD.ts
│  ├─ HatcheryHUD.ts
│  ├─ ResultModal.ts
│  └─ SafeArea.ts
├─ data/
│  ├─ eggs.ts
│  ├─ mongles.ts
│  ├─ regions.ts
│  └─ upgrades.ts
├─ platform/
│  ├─ PlatformAdapter.ts
│  ├─ WebPlatformAdapter.ts
│  └─ AppsInTossAdapter.ts
└─ assets/
   ├─ models/
   ├─ audio/
   └─ ui/
```

---

## 14. 앱인토스 플랫폼 연동

게임 로직이 앱인토스 SDK에 직접 의존하지 않도록 한다.

```ts
interface PlatformAdapter {
  login(): Promise<{ userId: string }>;
  loadSave(): Promise<SaveData | null>;
  saveGame(data: SaveData): Promise<void>;
  submitLeaderboard(score: number): Promise<void>;
  showRewardedAd(placement: string): Promise<boolean>;
  share(payload: SharePayload): Promise<boolean>;
  haptic(type: 'light' | 'medium' | 'heavy'): Promise<void>;
  getServerTime(): Promise<number>;
}
```

- 개발 환경에서는 `WebPlatformAdapter`와 로컬 저장소 사용
- 앱인토스 샌드박스와 실제 출시 빌드에서는 `AppsInTossAdapter` 사용
- SDK 호출 실패가 게임 진행을 막지 않도록 예외 처리

### 14.1 출시 필수 연동

| 기능 | 사용 목적 | 구현 시점 |
|---|---|---|
| 게임 로그인 | 사용자 식별, 저장 및 리더보드 연결 | MVP |
| Safe Area | 노치·홈 인디케이터 대응 | MVP |
| 네이티브 저장소 | 진행 데이터 보존 | MVP |
| 행동 기록 | 튜토리얼·탐험·부화 이탈 분석 | MVP |
| 게임 리더보드 | 재방문과 경쟁 유도 | MVP |
| 햅틱 | 알 획득·균열·부화 피드백 | MVP |
| 화면 가시성 감지 | 백그라운드 일시정지 및 저장 | MVP |
| 서버 시간 | 일일 보상·이벤트·오프라인 진행 검증 | 출시 전 |

### 14.2 성장 및 수익화 연동

| 기능 | 게임 내 사용 |
|---|---|
| 리워드 광고 | 일일 구조권, 부화 가속, 원정 보상 2배 |
| 전면형 광고 | 원정 여러 회 종료 후 자연스러운 화면 전환에서만 제한적으로 사용 |
| 인앱 결제 | 외형, 슬롯, 편의 기능, 시즌 상품 |
| 공유 리워드 | 희귀 몽글이 결과 카드 또는 친구 도전 링크 공유 |
| 토스 포인트 프로모션 | 첫 부화, 일정 거리 도달 등 명확한 미션 달성 보상 |

광고 또는 결제 제안은 전설 알을 놓친 직후 감정적으로 압박하는 방식으로 노출하지 않는다.

### 14.3 리더보드 점수 정의

앱인토스 게임센터 리더보드는 미니앱당 하나를 기준으로 설계한다. MVP의 대표 점수는 `최대 원정 거리`로 한다.

```text
leaderboardScore = floor(maxSuccessfulReturnDistance)
```

- 알을 들고 기지 귀환에 성공한 거리만 인정한다.
- 단순히 멀리 뛰고 실패한 기록은 점수에 포함하지 않는다.
- 게임 종료 또는 귀환 결과 화면에서 점수를 제출한다.
- 점수는 클라이언트 조작을 고려해 서버 검증 가능 구조를 준비한다.
- 리더보드를 열기 직전에 현재 진행 상태를 저장하고 탐험을 일시정지한다.

### 14.4 필수 행동 분석 이벤트

```text
game_start
tutorial_step_complete
expedition_start
egg_discovered
egg_pickup
egg_drop
expedition_success
expedition_fail_timeout
egg_saved
hatch_start
hatch_manual_hit
hatch_complete
mongle_obtained
upgrade_purchase
leaderboard_open
share_attempt
rewarded_ad_complete
purchase_complete
```

각 이벤트에는 필요한 최소 속성만 포함하고 개인식별정보를 임의로 기록하지 않는다.

---

## 15. MVP 범위

### 15.1 반드시 구현

- 세로 1080 × 1920 대응 반응형 화면
- Three.js 쿼터뷰 맵
- 가상 조이스틱 이동
- 상황 행동 버튼
- 알 5종
- 몽글이 8종
- 탐험 구역 2개
- 알 접근 및 들고가기
- 알 무게에 따른 이동속도 감소
- 제한시간과 귀환 실패
- 알이 날아가는 실패 연출
- 기지 귀환 및 알 보관
- 부화실
- 수동 타격
- 자동 초당 타격
- 타격 피해와 타격 횟수 강화
- 균열 4단계
- 부화 결과 화면
- 도감 기본 화면
- 앱인토스 게임 로그인
- 앱인토스 Safe Area
- 앱인토스 네이티브 저장소와 개발용 로컬 저장 폴백
- 앱인토스 행동 분석
- 앱인토스 게임센터 리더보드
- 앱 가시성 변경 시 자동 일시정지 및 저장
- 햅틱 피드백
- 품질 설정

### 15.2 출시 직전 또는 MVP 이후

- 공유 카드
- 공유 리워드
- 토스 포인트 프로모션
- 리워드 광고
- 인앱 결제
- 비동기 친구 연구소 방문
- 몽글이 합성
- 시즌 알
- 추가 지역
- 오프라인 자동 진행

### 15.3 MVP에서 제외

- 실시간 PvP
- 실시간 멀티플레이 월드
- 플레이어 간 직접 거래
- 복잡한 전투
- 자유 카메라 회전
- 한 번에 여러 알 운반
- 50 × 50 × 50 대형 모델의 다수 동시 표시

---

## 16. 구현 순서

1. 앱인토스 공식 WebView 샘플과 현재 SDK 버전 확인
2. Vite + TypeScript + `@apps-in-toss/web-framework` 프로젝트 구성
3. 앱인토스 앱 등록 및 개발·샌드박스 실행 환경 구성
4. 1080 × 1920 기준 반응형 레이아웃과 Safe Area 구현
5. Three.js 기본 장면, 조명, OrthographicCamera 구현
6. 임시 복셀 플레이어와 바닥 생성
7. 가상 조이스틱과 플레이어 이동 구현
8. 카메라 추적 구현
9. 임시 알 배치와 근접 감지 구현
10. 상황 행동 버튼 및 들고가기 구현
11. 알 운반 속도 페널티 구현
12. 기지 귀환 구역 및 보관 구현
13. 탐험 타이머와 알 유실 연출 구현
14. 부화실 및 알 HP 구현
15. 수동 타격과 자동 DPS 구현
16. 강화 시스템 구현
17. 부화 결과와 몽글이 데이터 구현
18. 앱인토스 로그인·저장·행동 분석 연결
19. 게임센터 리더보드 연결
20. 샌드박스 실기기 테스트
21. 모바일 성능 최적화 및 출시 체크리스트 검증

---

## 17. 완료 조건

MVP는 다음 시나리오를 모바일 화면에서 처음부터 끝까지 수행할 수 있어야 한다.

1. 플레이어가 앱을 실행한다.
2. 튜토리얼 안내에 따라 조이스틱으로 이동한다.
3. 알 근처에서 `들고가기` 버튼이 나타난다.
4. 알을 들면 이동속도가 감소한다.
5. 시간 안에 기지로 돌아오면 알이 저장된다.
6. 너무 멀리 갔다가 시간이 끝나면 알이 날아간다.
7. 저장한 알을 부화실에서 선택한다.
8. 직접 두드리거나 자동 장비로 알의 HP를 감소시킨다.
9. 알 외형이 단계적으로 갈라진다.
10. HP가 0이 되면 몽글이가 부화한다.
11. 획득한 몽글이가 도감과 인벤토리에 저장된다.
12. 재화를 사용해 이동속도, 자동 타격 피해, 초당 타격 횟수를 강화한다.
13. 강화 결과로 이전보다 먼 지역의 알을 가져올 수 있다.

---

## 18. 첫 구현용 임시 수치

```ts
export const INITIAL_BALANCE = {
  expeditionDurationSeconds: 45,
  playerMoveSpeed: 4.5,
  interactionRadius: 1.4,
  returnZoneRadius: 2.2,
  manualTapDamage: 1,
  autoHitDamage: 1,
  autoHitsPerSecond: 1,
  maxEggInventory: 6,
  maxActiveMongles: 3,
  commonEggCarryMultiplier: 0.9,
  rareEggCarryMultiplier: 0.8,
  epicEggCarryMultiplier: 0.68,
  legendaryEggCarryMultiplier: 0.55,
  secretEggCarryMultiplier: 0.42,
};
```

이 수치는 프로토타입을 위한 시작값이며 플레이 테스트 후 조정한다. 밸런스 값을 코드 여러 곳에 하드코딩하지 말고 중앙 데이터 파일에서 관리한다.

---

## 19. 디자인 키워드

- 귀여운 미지의 생명체
- 장난감 같은 복셀
- 말랑한 애니메이션
- 위험하지만 밝은 탐험
- 알을 들고 도망치는 긴장감
- 블라인드박스 같은 부화 기대감
- 작고 북적이는 연구소
- 한눈에 읽히는 실루엣

피해야 할 방향:

- 공포스럽고 현실적인 괴물
- 지나치게 복잡한 UI
- 화면을 가리는 거대한 캐릭터
- 색만 바꾼 희귀 개체
- 자동사냥만 바라보는 수동적 진행
- 실패 직후 강제 결제창
- 다른 게임의 캐릭터·UI·맵을 그대로 복제한 표현

---

## 20. Codex 첫 작업 요청문

### 2026-09-23 사용자 추가 결정 — 이전 충돌 항목보다 우선

#### 최종 추가 규칙

- 밤에도 농장 내부 이동은 허용하며 탐험 출구만 물리적으로 차단한다.
- HP 0은 즉시 자동 복귀가 아닌 사망 연출과 선택 화면으로 이어진다. 무료 기지 부활 또는 10초 가상 광고 후 현장 부활을 제공한다. 밤에는 현장 부활도 기지로 제한한다.
- 운동을 시작하면 러닝 애니메이션과 카메라 확대를 적용하고 `운동 내리기`를 누르기 전까지 이동을 잠근다.
- 스피드는 m/s 없이 숫자로 표시한다. 트레일 배율을 별도 HUD 수치로 나열하지 않고 실제 초당 운동 증가량에 반영한다. 원래 +10이면 트레일 2배 적용 후 +20으로 표시한다. 현재 밸런스의 기본 운동 증가량은 0.01이며 이 요청으로 기본값 자체를 10으로 변경하지 않는다. 장착 스피드 펫 효과도 실제 증가량에 반영한다.
- 운동 중 매초 실제 증가량을 캐릭터 위 숫자 이펙트로 표시한다. 계산에 배율을 중복 적용하지 않는다.
- 숫자 표기는 10,000,000을 한 단위로 축약한다: 10^7=1A, 10^14=1B, 10^182=1Z, 10^189=1AA. 이후 AB 순서다. 부동소수점 잔여 자릿수는 노출하지 않는다.
- 농장 펫 메뉴에서 최대 3종의 착용을 관리한다. 도감은 펫 메뉴에서 접근한다. 농장 판매 스토어에서 알 또는 펫을 선택·확인 후 판매한다. 마지막 착용 펫을 팔면 자동 해제하며 도감 발견·보상 이력은 보존한다.

- 미니앱의 기본 조작 버튼은 공격. 가까운 알은 들고가기, 운반 중이면 내려놓기, 운동기구 근처면 운동 시작/종료가 우선한다. 선택된 알 하나에만 빨간 외곽선을 표시한다.
- 밤은 접속 후 경과시간이 아닌 공통 UTC epoch 기준 180초 주기, 매 주기의 첫 10초다. 토스에서는 서버 시각, 멀티플레이는 로컬 게임 서버 시각, 독립 브라우저는 기기 시각을 사용한다. 밤에 탐험 영역 전체를 검은 직육면체 장벽으로 덮고 이동/알 줍기를 막는다. 운반 알 낙하 후 농장으로 즉시 귀환, 10초 후 해제. 계정·펫·강화가 밤의 시각을 바꾸지 않는다.
- 탐험의 바람 제한시간은 별도 45초이며 시간 강화만 적용한다. 펫의 `시간 +15초` 같은 능력은 폐지한다. 펫 능력은 클릭·오토·스피드 중 하나의 배율(1.1~1.7배)이고 장착 펫끼리 곱한다. 보유만 한 펫은 농장에서 놀지만 능력을 적용하지 않는다.
- 펫 머리 위에는 배경 패널 없이 `[등급] 이름`과 `능력` 두 줄을 띄운다. 실제 몸체 높이에 붙이고 빛기둥 높이를 이름표 기준으로 삼지 않는다. 외곽선으로 글자 대비를 확보한다.
- 알 크기 C/B/A/S/SS/SSS/SECRET = 5/10/20/35/55/75/100. 둥지도 비례하며 알은 둥지 바닥에 놓인다. 펫 제작 격자는 10³~100³까지 허용한다. 100³ 모델은 AIvoxel 부분 모델을 합성하며 개별 복셀 Mesh는 금지한다.
- 보스는 평소 잠들고 알을 훔치면 추격한다. 공격 예고 후 명중하면 운반 알을 즉시 떨어뜨린다. 피해 = 지역별 고정 피해 + 최대 HP × 비례 계수. 일반 계수 3~8%, 최종 20단계 강공격만 20% 상한. 현재 5개 지역에는 3/4/5/6/8%를 적용한다.
- 최대 HP 100, 체력 강화당 +20. 3초 미피격 이후 초당 최대 HP 4% 재생, 다시 맞으면 대기시간 초기화. 기지 도착 시 전량 회복. HP 0이면 기지 복귀하며 알은 피격 위치에 남는다.
- 기본 농장에는 보관함을 노출하지 않고 부화실 안에 배치한다. 귀환 성공 시 중앙 획득 안내와 Y축 회전 알 프리뷰, 이펙트를 보여준다. 첫 실행 5단계 튜토리얼을 제공한다.
- 도감의 미발견은 이미지/이름/능력을 `???`로 숨긴다. 발견 펫 클릭 보상, 지역 완성 보상, 전체 완성 보상은 각각 한 번만 저장한다. 등급 확률은 모든 지역에서 45/28/15/7/3.5/1.3/0.2%다.
- 농장 운동은 초당 영구 이동속도 0.01 증가, 별가루 모터 강화당 초당 증가량 +0.01. 트레일은 1/1.15/1.35/1.65배 중 하나만 장착한다.
- 실시간 멀티플레이 금지 조항은 최신 요청으로 변경한다. 현재는 로컬 게스트 서버에서 위치/외형/배트 공격을 동기화한다. 배트 판정은 서버의 거리·전방·쿨다운 검사, 피격 시 밀침/넘어짐/알 낙하. 공유 낙하 알은 서버가 한 명에게만 줍기를 허용한다. 정식 계정 인증·서버 권위 경제·클라우드 저장은 별도 출시 조건이다.
- 수익화 흐름은 실제 광고 SDK가 아닌 가상 광고로 먼저 검증한다. 상점 버튼 → “이것은 가상 광고입니다.” → 10초 동안 닫기 숨김 → 오른쪽 위 닫기 → 별가루 30개 1회 지급. 실제 광고 매출·노출·정산으로 보고하지 않는다.
- 20세계·140펫·60알 외형의 정식 목표와 최초 4세계·28펫·12알 외형은 `docs/qa/stage-roadmap.md`의 제작 기획으로 관리한다. 기존 5지역·100종 저장 데이터는 유지하며 출시 카탈로그 전환 전 마이그레이션 검증이 필요하다. 환경 방해/조합 저항/19~20 엔딩 연출은 아직 제작 단계다.

아래 요청으로 구현을 시작한다.

> 이 저장소에서 `alkong-expedition-game-spec.md`를 전체 구현 기준으로 사용해 **앱인토스 게임 미니앱용 WebView MVP**를 만들어라. 먼저 기존 프로젝트 구조와 패키지를 점검하고, 앱인토스 공식 샘플과 현재 개발 문서를 기준으로 `@apps-in-toss/web-framework`, Vite, TypeScript, Three.js 구성을 준비하라. iframe은 사용하지 마라. 첫 단계에서는 앱인토스 Safe Area를 반영한 세로형 반응형 캔버스, 쿼터뷰 OrthographicCamera, 임시 복셀 캐릭터, 가상 조이스틱 이동, 카메라 추적, 임시 알 접근 감지, 상황별 `들고가기/내려놓기` 버튼, 알 운반 시 이동속도 감소, 기지 귀환 구역까지 구현하라. 앱인토스 환경 밖에서는 개발용 폴백 어댑터로 실행되게 하고, 모바일 터치와 데스크톱 키보드 입력을 모두 지원하라. 복셀 하나마다 개별 Mesh를 생성하지 마라. 구현 후 타입 검사, 테스트, 모바일 뷰포트 검증을 실행하고 변경 파일과 검증 결과를 보고하라.
